/*jshint expr: true*/
/*
define(['views/tracklist.view'], function(Tracklist) {
    describe('TrackList View', function() {
        var region;
        before(function() {
            $('<div id="test" />').appendTo('body');
        });
        beforeEach(function() {
            region = new Backbone.Marionette.Region({
                el: "#test"
            });
        });
        it('Shows no data message', function() {
            var tracklist = new Tracklist();
            region.show(TrackList);
        });
    });
});
*/